@Library('jenkins_lib') _
pipeline {                      # sample
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Working on process..'
                code('faizan')
            }
        }
    }
}

