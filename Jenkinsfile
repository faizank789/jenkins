@Library('jenkins_lib') _
pipeline {                     
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Welcome'
                test('faizan')
                school('faian', 'kampoo')
            }
        }
    }
}

