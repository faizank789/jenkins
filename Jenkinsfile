@Library('jenkins_lib') _
pipeline {                     
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Working on process..'
                test('faizan')
            }
        }
    }
}

