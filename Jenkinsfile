@Library('jenkins_lib') _
pipeline {                     
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Working for you..'
                test('faizan')
            }
        }
    }
}

