@Library('jenkins_lib') _
pipeline {                     
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'process'
                test('faizan')
            }
        }
    }
}

